# Template — the wizard renders this dynamically from the hardware profile.
# This file is for reference / hand-install only; do NOT copy verbatim.
# The actual generated drop-in lives at /etc/default/grub.d/90-hyphaed.cfg

# Example (i9-14900KF, 8P/16E, RTX 5070, GNOME+Wayland):
#
# GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} \
#   iommu=pt intel_iommu=on,igfx_off \
#   intel_pstate=active \
#   transparent_hugepage=madvise \
#   split_lock_detect=warn \
#   mitigations=auto \
#   nohz_full=0-15 rcu_nocbs=16-31 \
#   nvidia-drm.modeset=1 \
#   nvidia.NVreg_PreserveVideoMemoryAllocations=1 \
#   nvidia.NVreg_EnableGpuFirmware=1"
