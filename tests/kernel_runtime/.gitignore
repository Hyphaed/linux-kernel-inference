# Built from the RUNNING kernel's BTF and headers — correct only for the
# kernel that produced them, so they are regenerated by `make`, never committed.
vmlinux.h
cache_ext_probe.bpf.o
cache_ext_probe
dmabuf_ioctl_probe
