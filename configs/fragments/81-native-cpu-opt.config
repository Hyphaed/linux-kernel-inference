# Native CPU optimization — i9-14900KF (Raptor Lake) on this build machine.
# Passes -march=native to GCC, enabling AVX2, FMA3, BMI2, AES-NI, SHA-NI
# and all other ISA extensions present on this CPU.
# The resulting kernel is NOT portable to other x86-64 machines.
CONFIG_X86_NATIVE_CPU=y
