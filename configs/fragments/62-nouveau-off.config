# Nouveau fights nvidia-drm on Wayland — always off when the 595 driver drives the dGPU.
# Decoupled from 61-no-amdgpu-no-nouveau.config so the AMD iGPU path (which
# skips 61) still disables nouveau.
CONFIG_DRM_NOUVEAU=n
CONFIG_DRM_NOUVEAU_BACKLIGHT=n
