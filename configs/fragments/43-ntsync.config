# NTSYNC: Windows NT synchronization primitives (futex-like)
# Required for Proton/Wine NT-aware synchronization — lower latency than
# user-space esync/fsync shims. Upstream since 6.14. Enable as module so
# it loads only when Proton requests it.
CONFIG_NTSYNC=m
