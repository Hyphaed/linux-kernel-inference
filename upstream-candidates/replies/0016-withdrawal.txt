From: Ferran Duarri <ferran.duarri@me.com>
Subject: Re: [PATCH] mm: thp: default defrag mode to defer+madvise

Please drop this one. The review is right and my commit message has the
mechanism backwards.

I claimed:

> Paired with transparent_hugepage=always every anonymous fault
> becomes eligible, and under memory pressure the faulting thread can
> stall in compaction.

That does not happen. In vma_thp_gfp_mask(), the current default:

	/* Only do synchronous compaction if madvised */
	if (test_bit(TRANSPARENT_HUGEPAGE_DEFRAG_REQ_MADV_FLAG, ...))
		return GFP_TRANSHUGE_LIGHT |
		       (vma_madvised ? __GFP_DIRECT_RECLAIM : 0);

A non-madvised fault gets GFP_TRANSHUGE_LIGHT with no reclaim flag at
all, so it fails fast and cannot stall in direct compaction. The stall I
described is only reachable from a MADV_HUGEPAGE region -- and
defer+madvise keeps __GFP_DIRECT_RECLAIM for exactly those regions:

	if (test_bit(TRANSPARENT_HUGEPAGE_DEFRAG_KSWAPD_OR_MADV_FLAG, ...))
		return GFP_TRANSHUGE_LIGHT |
			(vma_madvised ? __GFP_DIRECT_RECLAIM :
					__GFP_KSWAPD_RECLAIM);

So the patch removes no stall. What it actually changes is the other
branch: non-madvised faults gain __GFP_KSWAPD_RECLAIM, which they did
not have. That is strictly more background work, waking kswapd and
kcompactd on failed THP allocations across every anonymous fault under
THP=always. The patch does close to the opposite of what it claims, and
on a fragmented machine it is a plausible regression rather than an
improvement.

Documentation/admin-guide/mm/transhuge.rst says the same thing I should
have read before writing the commit message: madvise "will enter direct
reclaim like always but only for regions that are have used
madvise(MADV_HUGEPAGE)".

I also had no measurement, and said so in the commit message -- the
machine never reached the condition: thp_fault_fallback was 0 across
60682 huge-page faults, so direct compaction was never entered and
compact_stall 0 testified to nothing. With the correctness argument gone
too, there is nothing holding this up.

If I come back to it, it will be with fault-latency percentiles under
real memory pressure at madvise versus defer+madvise on a machine that
actually reaches fallback, and a commit message describing the
kswapd-wakeup tradeoff rather than a stall that the code does not incur.

Sorry for the noise, and thanks to whoever pointed the review at it --
it caught a real error.

Ferran
